"""fieldproof: document extraction that shows its work.

Every extracted field is linked to the exact words it came from, so a human
reviewer (or a downstream system) can tell a verified value from a
hallucinated one before it reaches a database.
"""

__version__ = "0.1.0"
